function digite(){
    let n = Number(prompt('Digite um número inteiro qualquer:'))
    saida.innerHTML = alert(`Antes de ${n}, temos o número ${n-1}.
Depois de ${n}, temos o número ${1+n}.`)
}

    
